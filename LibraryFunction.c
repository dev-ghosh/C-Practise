#include<stdio.h>
#include<math.h>
int main(){
    // int a;
    // printf("enter a no :");
    // scanf("%d",&a);
    // int root = sqrt(a);
    // printf("the square root of the no is:%d",root);
    int q =pow(2,4);
    printf("%d",q);
        return 0;
}